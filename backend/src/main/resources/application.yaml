spring:
  application:
    name: Price-Tracker-Project

  datasource:
    url: jdbc:mysql://localhost:3306/price_tracker
    username: root
    password: Sameer@2002
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
